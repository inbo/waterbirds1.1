% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/give_dropping_herbivores.R
\name{give_dropping_herbivores}
\alias{give_dropping_herbivores}
\title{Calculate dropping model for herbivores}
\usage{
give_dropping_herbivores(
  species_name,
  n_individuals,
  n_days,
  var_season,
  var_species = read.csv2(system.file("input_variables/var_species.csv", package =
    "waterbirds1.1")),
  terr_food_herbivores =
    read.csv2(system.file("input_variables/terr_food_herbivores.csv", package =
    "waterbirds1.1")),
  n_drop = 45.02,
  p_drop = 6.18
)
}
\arguments{
\item{species_name}{(vector of) scientific name(s) of the herbivorous
waterbird species (without author name)}

\item{n_individuals}{(vector of) number of individuals of the species that
are present on the water body}

\item{n_days}{(vector of) number of days that the individuals of the species
are present on the water body}

\item{var_season}{(vector of) season(s) in which the given individuals of the
species are present on the water body.
Possible values are \code{spring}, \code{summer} and \code{winter}.}

\item{var_species}{table (data.frame) with waterbird data with at least
columns \code{species} (scientific name), \code{body_mass} (weight in g) and
\code{diet} with possible values "herbivore",
"vertebrates" (= carnivore with vertebrate diet, e.g. fish) and
"mix (in)vertebrates" (= carnivores with a mixed diet of vertebrates and
invertebrates, e.g. fish and insects).
Defaults to the table provided in this package that can be accessed by
\code{read.csv2(system.file("input_variables/var_species.csv", package = "waterbirds1.1"))}.}

\item{terr_food_herbivores}{table (data.frame) with the species and
season-specific proportion of energy obtained from terrestrial food
relative to the total amount of energy required ($f_t$), with at least
columns \code{species} (scientific name), \code{season} ("spring", "summer" or
"winter") and \code{f_t}.
Defaults to the table provided in this package that can be accessed by
\code{read.csv2(system.file("input_variables/terr_food_herbivores.csv", package = "waterbirds1.1"))}.}

\item{n_drop}{elemental concentration of nitrogen (N) in droppings in mg/g
(defaults to 45.02 taken from article of \emph{Hahn et al., 2008})}

\item{p_drop}{elemental concentration of phosphorus (P) in droppings in mg/g
(defaults to 6.18 taken from article of \emph{Hahn et al., 2008})}
}
\value{
data.frame with columns \code{species_name}, \code{n_individuals} and
\code{var_season} as reference columns from the input,
and columns \code{n_tot} and \code{p_tot} with the nitrogen and phosphorus input
in kg by the birds in the given number of days.
}
\description{
This function quantifies the allochthonous nitrogen and phosphorus input
by herbivorous waterbirds based on daily faecal output and digestive
performance (dropping model, \emph{Hahn et al., 2008}).
}
\examples{
library(waterbirds1.1)
give_dropping_herbivores(
  species_name = c("Anas crecca", "Anas platyrhynchos"),
  n_individuals = c(1, 1),
  n_days = c(1, 1),
  var_season = c("spring", "winter")
)
}
\references{
\itemize{
\item Hahn S., Bauwer S., Klaassen M. (2008). Quantification of allochthonous
nutrient input into freshwater bodies by herbivorous waterbirds.
Freshwater Biology 53: 181-193.
\doi{10.1111/j.1365-2427.2007.01881.x}
}
}
\seealso{
Other herbivores: 
\code{\link{give_intake_herbivores}()}
}
\concept{herbivores}
